These three files were an early, never-wired data export (nothing in `Scripts/`
reads them). They are kept for reference only. Live moddable data is the
`GameData/` folder next to the executable, documented in the README and
exported by the editor; see `Scripts/Systems/GameDataLoader.cs`.
